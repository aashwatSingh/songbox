# Dated GPU price table. Every job's cost accounting (spec §9) reads from this file — never
# hardcode a price inline. Add a new dated entry when a provider changes pricing; don't overwrite
# old entries, so historical job costs stay reproducible.

providers:
  - name: modal
    gpu_type: A10
    price_per_second_usd: 0.000306
    effective_date: "2026-08-30"
